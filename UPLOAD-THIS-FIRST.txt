AGENT-X v5.11.4 — COSMETIC ONLY
===============================

YOUR CODE WAS ALREADY CORRECT. Verified against your live repo:
  version.json .......... 5.11.3
  MCP tool names ........ 16, ZERO dots, tools.0 = agentx_queue_topic
  invalid exports ....... 0
  [...rest] folder ...... deleted

The dots you saw were DOCUMENTATION TEXT on the Settings page
(web/components/McpPanel.tsx) — never sent to Claude. Now corrected so it
stops being misleading.

THE REAL REMAINING STEP — Claude caches the tool list per connector:

  1. Claude -> Settings -> Connectors
  2. DELETE the Agent-X connector completely (do not just toggle it off)
  3. Add custom connector:
       Name: Agent-X
       URL:  https://agent-x-two.vercel.app/api/mcp
       Both OAuth boxes: EMPTY
  4. Authorise when the sign-in page appears

Deleting is what forces Claude to re-fetch the tool list. Toggling off/on
reuses the cached (dotted) one, which is why the same error kept appearing.

These 3 files are optional polish — upload whenever convenient:
  web/app/api/mcp/route.ts
  web/components/McpPanel.tsx
  web/version.json
