AGENT-X v5.11.3 — FIXES THE FAILING VERCEL BUILDS
=================================================

WHY YOUR LAST 4 DEPLOYS ERRORED:
I added `export { handle };` to web/app/api/mcp/route.ts in v5.11.1.
Next.js only allows a route file to export GET/POST/OPTIONS and config keys.
Any other export = build failure. So v5.11.2 (the tool-name fix) never went
live, and the connector kept failing with the SAME error against fixed code.

DO THIS, IN THIS ORDER:

  1. DELETE the folder:  web/app/api/mcp/[...rest]
     (it holds a 1-byte junk file and is not needed)

  2. UPLOAD these 2 files:
       web/app/api/mcp/route.ts     <-- build fix + correct tool names
       web/version.json             <-- 5.11.3

  3. WATCH VERCEL until a deployment shows "Ready" (not "Error").

  4. Refresh the dashboard. The banner must read:
         web v5.11.3 · worker v5.11.3
     If web still says 5.11.0, the build has not succeeded — do not connect yet.

  5. ONLY THEN re-enable the Agent-X connector in Claude.
     URL exactly: https://agent-x-two.vercel.app/api/mcp
