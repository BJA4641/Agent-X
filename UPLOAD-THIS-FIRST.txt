AGENT-X v5.11.10 — RED BANNER + RENDER OOM
==========================================

!! DO THIS FIRST — NO FILE UPLOAD NEEDED !!

  RAILWAY IS RUNNING v5.11.6. YOUR REPO IS AT v5.11.9.
  Railway never rebuilt. Every pipeline fix since v5.11.6 is NOT deployed.

  Railway -> your Agent-X service -> Deployments -> Redeploy (latest commit)

  That alone fixes half the red banner.

THEN upload these 9 files:
  pipeline/agentcore/worker.py                    <-- heartbeat from claim loop
  pipeline/workers/departments/ops.py             <-- VERSION_HINT
  pipeline/agent/composer.py                      <-- OOM fix (zoompan surface)
  pipeline/agent/visuals.py                       <-- real images, not gradients
  pipeline/agent/grader.py
  pipeline/workers/departments/art_director.py    <-- real scenes per niche
  pipeline/workers/departments/editorial.py
  pipeline/tests/test_v5101_ratelimit.py
  prompts/script_v3.md
  web/version.json                                <-- 5.11.10

WHY THE BANNER LIED:
  At 02:15 your worker had written a provider report 3 minutes earlier, but
  worker_health had been frozen for 29 minutes. Liveness depended on a
  background thread that is not running in the deployed build. It now beats
  from the job claim loop — the one thing that must run for the worker to work.
