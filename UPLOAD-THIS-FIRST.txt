AGENT-X v5.11.17 — TWO LIVE ERRORS, FIRING EVERY TICK
=====================================================

Found through the MCP connector (agentx_failures), not guesswork:

  coo: "format mix skipped: name '_dt' is not defined"
       -> my v5.11.11 bug. The ENTIRE content mix never ran.
          This is why you still had 0 carousels and 0 stories.

  coo: "carousel spawn skipped: Priority has no attribute 'MEDIUM'"
       -> Priority.MEDIUM never existed. Every carousel spawn threw
          and was swallowed by a try/except, so no carousel was EVER made.

Both were downgraded to warnings by their guards, which is why they ran
unnoticed for hours.

UPLOAD 4 FILES:
  pipeline/workers/departments/portfolio.py   <-- both fixes
  pipeline/workers/departments/creative.py    <-- topic recovery
  pipeline/tests/test_v5101_ratelimit.py
  web/version.json                            <-- 5.11.17

After deploy, expect within ~2 minutes:
  coo: "🎛️ format mix for @puppy.parent: 1x carousel, 5x story"
If you see that line, the mix is finally running.
