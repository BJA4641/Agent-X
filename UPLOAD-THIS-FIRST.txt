AGENT-X v5.11.0 — THE STALE-DASHBOARD FIX
=========================================

UPLOAD THESE 3 FILES:

  1. web/lib/supabase/server.ts                  <-- THE FIX. Stops Next.js
                                                     caching database reads.
  2. web/version.json                            <-- 5.11.0
  3. pipeline/workers/departments/portfolio.py   <-- STILL MISSING from v5.10.7.
                                                     This is what CI is red about.

After Vercel redeploys, the banner should read:
    web v5.11.0 · worker v5.11.0 · beat <60s

If it still shows an old worker version, hard-refresh once (Ctrl+Shift+R) —
your browser may hold the old /api/version response.
