AGENT-X v5.11.19 — MCP CONNECTOR WAS LYING
==========================================

You asked me to verify the connector against Supabase. It failed:

    metric           MCP     Supabase    error
    board cleared    984     7,402       7.5x under
    jobs done        987     40,554      41x  under
    jobs queued      3       25          8x   under
    jobs in_progress absent  5           not reported at all

MCP board total = 1000 exactly. MCP jobs total = 1000 exactly.
The tools ran select() with no limit; PostgREST caps at 1000 rows; the code
counted the rows it got back. Every count saturated silently.

This matters because the connector is meant to be the ONLY diagnostic surface
for users who never touch the database.

FIXED: exact head-counts (count:"exact", head:true) — transfers no rows, so it
is cheaper as well as correct. All 5 MCP queries audited and bounded.

UPLOAD 2 FILES:
  web/app/api/mcp/route.ts
  web/version.json          <-- 5.11.19

AFTER DEPLOY: delete and re-add the connector (Claude caches the tool list),
then ask me to run agentx_pipeline_state. jobs done should read ~40,000+.
