# AGENT-X MASTER LEDGER
**Single source of truth. Append-only. Nothing is removed without founder approval.**
This file lives in the repo (docs/LEDGER.md) so it survives chat/context resets —
that is itself a decision (DEC-026). Every session appends; never rewrite from scratch.

Program objective: a production-ready autonomous AI-native media company managing
hundreds of profitable brands simultaneously — minimizing cost, maximizing content
quality, meeting daily publishing SLAs, and continuously improving itself.

Status legend: ✅ Implemented · 🟡 Scheduled in Roadmap · 🔵 Waiting for founder approval · 🔴 Blocked

---

## 1 · REQUEST TRACKER

| ID | Description | Priority | Status | Phase | Notes |
|---|---|---|---|---|---|
| REQ-GOV-1 | Demand-driven ideation governor (stop churn loop: 7,404 cleared/7d, ~$12 waste) | P0 | ✅ 2026-07-23 | v5.9.5 | Windowed idempotency `ideate:{acct}:{bucket}`, IDEATE_COOLDOWN_S=1800 |
| REQ-SLA-1 | Publishing SLA engine: daily plan, monitor, breach escalation, stuck-job self-heal | P0 | ✅ 2026-07-23 | v5.9.5 | sla.plan_day + sla.monitor; settings.sla_plan / sla_status |
| REQ-FAIR-1 | Fair job claiming — no account starves another; SLA-behind accounts jump queue | P1 | ✅ 2026-07-23 | v5.9.5 | fair_claim_order in agentcore/jobs.py, CAS untouched (DEC-023) |
| REQ-PAUSED-1 | Paused accounts compound resume-ready prep at exactly $0 | P1 | ✅ 2026-07-23 | v5.9.5 | paused.prep_cycle, free_chat only, board status='prep' (DEC-024/025) |
| REQ-TOPIC-1 | Kill the `write_script: no topic` fatal class (9 in 7d) | P0 | ✅ 2026-07-23 | v5.9.5 | Root cause: sweep read payload, topic lives in the topic COLUMN; + plan_one guard |
| REQ-OVERHEAD-1 | Cut ops overhead (human_desk 20s cadence = 14,370 jobs/wk) | P2 | ✅ 2026-07-23 | v5.9.5 | HUMAN_DESK_SYNC_SECONDS=120 → ~2,400/wk |
| REQ-SCHEMA-1 | Canonical db/schema.sql regenerated from LIVE DB | P1 | ✅ 2026-07-23 | parallel session | 41 tables, RLS enables included, generated by Postgres itself |
| REQ-PAUSED-2 | Prep quality pass: grade banked prep items with free grader; drop <6.0 | P2 | 🟡 Batch #2 | — | Discovered during v5.9.5 design |
| REQ-BUDGET-2 | Per-account monthly $25 cap surfaced in web UI with spend bar | P2 | 🟡 Batch #2 | — | Cap enforced since v5.9.4 (ceo_decide); UI missing |
| REQ-SCALE-WORKERS | 2nd Railway worker replica (fairness + CAS already multi-worker-safe) | P2 | 🟡 Batch #2 | — | Verify no double-heartbeat first |
| REQ-PREP-PROMOTE | On account resume: auto-promote banked 'prep' items into the live funnel | P1 | 🟡 Batch #2 | — | Bridge prep → idea with re-plan |
| REQ-SLA-UI | Studio: SLA status chips + 'prep' filter chip; surface settings.sla_status | P2 | 🟡 Batch #2 | — | Web layer only |
| REQ-PUB-TOKENS | Live publishing OAuth (IG/TikTok/YouTube tokens) | P1 | 🔴 Blocked | — | Needs founder to complete platform OAuth setups |
| REQ-FREEKEY-1 | Add GROQ / OPENROUTER key for free-council depth | P2 | 🔴 Blocked | — | Founder action in Railway Variables |
| REQ-TRADEMARK | Resolve "Agent-X" vs agentx.so collision | P1 | 🔵 Founder decision | — | Blocks public advertising |
| REQ-MCP-CLAIMS | Scope MCP integration claims honestly pre-marketing | P2 | 🔵 Founder review | — | No inflated capability claims |
| REQ-PAYOUT-1 | Stripe Connect payout automation for 50% affiliate commissions | P2 | 🟡 Phase 2+ | — | Currently manual |


## 1B · REQUEST TRACKER — FULL SCHEMA (v5.9.6, closes gap G-1)
Mandated fields: Description · Priority · Status · Phase · Dependencies · Estimated Effort ·
Business Impact · Engineering Impact · Completion Date · Notes.
(§1 above is retained verbatim as the historical short-form view — nothing removed, per standing rule 2.)

| ID | Description | Pri | Status | Phase | Dependencies | Effort | Business Impact | Engineering Impact | Completed | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| REQ-LADDER-FLOOR | Merge free ladder with hardcoded floor; never replace; shrink alert | P0 | ✅ | 0 | none | 1 pt | Extreme — restores 3 dead free rungs at $0 | Removes an entire failure class (autonomous config narrowing) | 2026-07-24 | WSJF 39.0, highest in plan. DEC-034 |
| REQ-ESCALATE-1 | SLA-aware paid escalation rung above free-exhausted | P0 | ✅ | 0 | REQ-LADDER-FLOOR | 3 pt | Extreme — converts a full wallet into published posts | Adds missing upward rung; all guards retained | 2026-07-24 | Pure fn `escalation_allowed` unit-tested. DEC-028 |
| REQ-GOV-2 | Age out stalled items from in-flight demand count | P1 | ✅ | 0 | none | 2 pt | Very high — unfreezes ideation when downstream stalls | Governor becomes stall-tolerant | 2026-07-24 | INFLIGHT_MAX_AGE_H=6. DEC-035 |
| REQ-DEDUPE-1 | Idempotency key on re-plan → write_script spawn | P1 | ✅ | 0 | none | 2 pt | Very high — prevents 5x spend multiplication post-escalation | Consistency with existing idempotency discipline | 2026-07-24 | `replan:{item_id}` |
| REQ-HEALTH-1 | Heartbeat from daemon thread; staleness no longer job-blocked | P1 | ✅ | 0 | none | 2 pt | High — restores trust in every health signal | Liveness decoupled from the queue it monitors | 2026-07-24 | Creates TD-06 (accepted). DEC-036 |
| REQ-PREPOBS-1 | Surface silent prep/provider skips | P2 | ✅ | 0 | none | 2 pt | Medium — dead free tier no longer looks like idleness | Kills a silent-failure path | 2026-07-24 | settings.prep_last_skip |
| REQ-SLA-TZ | SLA deadline → 14:00 Asia/Dubai, per-account overridable | P1 | ✅ | 0 | none | 2 pt | High — SLA finally measures the mandated deadline | Timezone resolution + per-account config | 2026-07-24 | Closes finding N-10 |
| REQ-DIAG-1 | Stop truncating diagnostics (150→900); report unattempted rungs | P1 | ✅ | 0 | none | 1 pt | High — prevents repeat multi-session blind debugging | Full-fidelity failure evidence | 2026-07-24 | NEW, discovered during Batch 1. TD-12 |
| REQ-E2E-1 | End-to-end integration test ideate→script vs stub provider | P1 | ✅ | 0 | REQ-ESCALATE-1 | 3 pt | High — would have caught zero-output on day one | Proves the loop, not just the parts | 2026-07-24 | Fails on v5.9.5 by design |
| REQ-KEYS-2 | Replace expired Groq key (HTTP 403) | P0 | ✅ | 0 | founder | 1 pt | Extreme — restores a free writing rung | none (ops) | 2026-07-23 | Founder-completed |
| REQ-OPENROUTER-1 | OPENROUTER_API_KEY present | P0 | ✅ | 0 | founder | 1 pt | High — unlocks 3 free routes | none (ops) | 2026-07-23 | Confirmed in provider_inventory |
| REQ-SLASTAGE-1 | Back-planned per-stage deadlines stamped on queued jobs | P1 | ✅ | 1 | REQ-SLA-TZ ✅ | 3 pt | High — fairness becomes SLA enforcement | One central stamping site | 2026-07-24 | v5.9.7. DEC-031 |
| REQ-AUTOAPPROVE-1 | Opt-in auto-approve above a strict grade bar | P1 | ✅ | 1 | none | 3 pt | High — removes founder from every publish path | Approval gate, fails closed | 2026-07-24 | v5.9.7, OFF by default. DEC-038 |
| REQ-COSTPOST-1 | Cost per PUBLISHED post metric (today + all-time) | P1 | ✅ | 1 | none | 2 pt | High — the ROI denominator | Read-only aggregation | 2026-07-24 | v5.9.7, settings.cost_per_post |
| REQ-WEB-404 | Sidebar links /dashboard/store, /digital, /affiliate return 404 — pages never built | P1 | 🟡 | 1 | none | 3 pt | High — founder-visible breakage, requested twice | 3 Next.js pages + data wiring | — | DISCOVERED by G-5 verification |
| REQ-VERSION-1 | Single source of truth for version (web/version.json) — three constants collapsed to one | P1 | ✅ | 1 | none | 2 pt | High — deploy verification was impossible; banner reported 5.9.4 while 5.9.7 ran | Removes 3-way version drift | 2026-07-24 | v5.9.8. Founder-reported. DEC-039 |
| REQ-WEB-404 | Build /dashboard/store, /digital, /affiliate (sidebar linked to 404s) | P1 | ✅ | 1 | none | 3 pt | High — founder-visible breakage, reported twice | 3 pages + graceful API-absent handling | 2026-07-24 | v5.9.8. TD-13 closed |
| REQ-REPO-HYGIENE | Archive 32 loose root .md/.txt files; delete stray files inside web/app | P2 | ✅ | 1 | none | 1 pt | Low direct, high clarity | Removes build-risk files from the Next.js app dir | 2026-07-24 | v5.9.8. Founder-reported. DEC-040 |
| REQ-HEALTH-2 | Heartbeat pulse must self-report (alive/writes/last_error) | P1 | ✅ | 1 | REQ-HEALTH-1 | 2 pt | High — a non-writing pulse looked identical to a working one | Removes a silent-failure path in telemetry itself | 2026-07-24 | v5.9.9. settings.heartbeat_pulse |
| REQ-ESC-OBS | Record every paid-escalation verdict + reason | P1 | ✅ | 1 | REQ-ESCALATE-1 | 1 pt | High — "writer stuck" vs "declined: account budget spent" were indistinguishable | Observability on the critical path | 2026-07-24 | v5.9.9. settings.escalation_last |
| REQ-LADDER-OBS | providers.probe publishes the live free-ladder report | P1 | ✅ | 1 | REQ-LADDER-FLOOR | 1 pt | High — which rungs are usable was only visible in job error strings | Operator-readable ladder state | 2026-07-24 | v5.9.9. settings.free_ladder_report |
| REQ-BUDGET-3 | Per-account daily budgets ($0.50/$0.75) are below the cost of one escalated post | P1 | 🔵 | 1 | founder decision | 1 pt | High — escalation will decline on budget grounds | Data change only, no code | — | DISCOVERED 2026-07-24 from live data |
| REQ-ARTDIRECTOR-1 | Art Director agent: shot-level prompt packs (subject/composition/lighting/camera/style/palette/negative) | P2 | ✅ | 3 | none | 5 pt | High — closes the 'AI made a video' vs 'AI directed a video' gap | New department, 1 free call/reel, fail-open | 2026-07-24 | v5.10.0. DEC-041 |
| REQ-UNITECON-1 | Publish unit-economics model + per-post cost target to guide pricing | P1 | ✅ | 1 | REQ-COSTPOST-1 | 2 pt | High — pricing decision depends on it | Analysis + doc | 2026-07-24 | docs/UNIT-ECONOMICS.md |
| REQ-RATELIMIT-1 | Per-provider token bucket, adaptive 429 backoff, skip-to-next-rung | P1 | ✅ | 2 | none | 5 pt | High — 38x HTTP 429 in 6h was collapsing free capacity in bursts | Thread-safe by construction; hard prerequisite for REQ-PARALLEL-1 | 2026-07-24 | v5.10.1. DEC-042 |
| REQ-VERSION-2 | Canonical version file must reach the worker image (Dockerfile COPY) | P1 | ✅ | 1 | REQ-VERSION-1 | 1 pt | Medium — worker reported 5.9.8 while 5.10.0 ran | Fallback is now a marker, not a plausible number | 2026-07-24 | v5.10.1. My own defect from v5.9.8 |
| REQ-PARALLEL-1 | claim(limit=N) + thread pool — execution was strictly sequential | P1 | ✅ | 2 | REQ-RATELIMIT-1 ✅ | 5 pt | Extreme at scale — one execution slot for 105 accounts made the SLA unreachable | Threads (network-bound work), same 512MB container, $0 infra | 2026-07-24 | v5.10.2. Rollback: WORKER_CONCURRENCY=1. DEC-029 |
| REQ-LANES-1 | Job lanes light(4)/heavy(1)/free_only(2) with per-lane semaphores | P1 | ✅ | 2 | REQ-PARALLEL-1 | 3 pt | Medium — renders would OOM a 512MB box if run wide | Lane classification + semaphores | 2026-07-24 | v5.10.2. DEC-030 |
| REQ-BACKOFF-RESET | Release writer jobs from "no model" backoff once the ladder recovers | P1 | ✅ | 1 | REQ-LADDER-OBS ✅ | 2 pt | High — jobs were serving 30-min sentences handed down while the free tier was dead | Pure health gate + targeted release | 2026-07-24 | v5.10.2. DISCOVERED from live data |
| REQ-KEYROTATE-1 | Rotate the Groq key exposed in a screenshot | P0 | 🔴 | — | founder | 1 pt | Security — key value visible in plain text | none | — | Flagged 2026-07-24 |
| REQ-ESCALATE-2 | brain.write_script must ACCEPT force_paid and route to the paid client | **P0** | ✅ | 0 | REQ-ESCALATE-1 | 2 pt | **Extreme — the approved escalation never spent a cent; this is why output is still zero** | Adds the missing rung; removes a silent free-retry degrade | 2026-07-24 | v5.10.3. My own defect from v5.9.6. DEC-043 |
| REQ-ESC-THROTTLE | Serialise paid escalation; reserve spend before the call; per-account hourly ceiling | **P0** | ✅ | 0 | REQ-ESCALATE-2, REQ-PARALLEL-1 | 3 pt | **Extreme — $1.27 burned in 15 min against a $2.50 DAILY budget** | Closes a race my own concurrency batch opened | 2026-07-24 | v5.10.4. DEC-044 |
| REQ-BANNER-DIAG | /api/version must expose what the WEB actually reads (server time, raw heartbeat, row count) | P1 | 🟡 | 1 | none | 2 pt | Medium — banner claims worker v5.8.8 / 30,673s while the DB holds ONE row at v5.10.3 lag 178s | Web-layer diagnostics | — | Next batch |
| REQ-MCP-CONNECTOR | Custom MCP server exposing Agent-X APIs so the assistant can read the site directly | P2 | 🟡 | 3 | first published post | 5 pt | Medium — removes screenshot round-trips | New service | — | Founder-requested |
| REQ-SPEND-DISPLAY | Workspace Spend card summed events[].cost_usd — a column agent_events never had | P1 | ✅ | 1 | none | 1 pt | Medium — card read $0.000 while the ledger held $1.27, hiding a live overspend | Server-side sum from run_ledger | 2026-07-24 | v5.10.5. Founder-reported |
| REQ-BANNER-DIAG | /api/version exposes worker rows, raw heartbeats, ladder, escalation verdict, recent errors + failed jobs | P1 | ✅ | 1 | none | 2 pt | High — "worker is not beating" gave no way to tell WHICH agent broke | Web diagnostics + no-store headers | 2026-07-24 | v5.10.5 |
| REQ-MCP-DIAG | 4 diagnostic MCP tools: diagnostics, failures, agent_chatter, pipeline_state | P1 | ✅ | 1 | existing /api/mcp | 3 pt | High — lets the assistant read agent conversations and errors directly | Extends the shipped MCP server | 2026-07-24 | v5.10.5. Founder-prioritised |
| REQ-OPENROUTER-CREDIT | OpenRouter balance is $0.00 — :free routes are throttled to near-zero | **P0** | 🔴 | 0 | founder | 1 pt | **High — 5 of 7 free rungs are effectively dead, forcing paid escalation** | none (billing) | — | provider_probe: "needs attention: openrouter, deepseek" |
| REQ-MCP-OAUTH | Full OAuth 2.1 for the MCP server: discovery metadata, Dynamic Client Registration, PKCE authorize, token + refresh | P1 | ✅ | 1 | REQ-MCP-DIAG | 5 pt | High — Claude.ai connectors CANNOT use static bearer headers; without OAuth the connector fails at registration | 4 new routes, 3 tables, 4 rewrites, MCP accepts axat_ tokens | 2026-07-24 | v5.10.6. DEC-045 |
| REQ-TOKEN-ROTATE-2 | MCP personal token exposed in chat — rotate in Settings → MCP | P1 | 🔴 | — | founder | 1 pt | Security | none | — | Flagged 2026-07-24 |
| REQ-LEDGER-DEDUPE | Paid calls were recorded twice (aisuite.text AND brain) — spend inflated 2x | P1 | ✅ | 1 | none | 1 pt | High — $2.97 shown vs ~$1.50 actually spent; trips budget guards early and doubles apparent cost-per-post | One call, one cost row | 2026-07-24 | v5.10.7. DEC-046 |
| REQ-BACKPRESSURE-1 | Pause production while drafts await founder approval (cap 5) | P1 | ✅ | 1 | none | 2 pt | High — 11 drafts were queued unapproved; a 12th has zero marginal value and real cost | Gate before demand calc | 2026-07-24 | v5.10.7. DEC-047 |
| REQ-MCP-ACTIONS | MCP write tools: account_control, budget_control, retry_failed | P2 | ✅ | 1 | REQ-MCP-OAUTH | 3 pt | Medium — lets the assistant operate the platform, not just observe it | 3 tools, all echo before/after | 2026-07-24 | v5.10.7, founder-approved |
| REQ-CHEAP-TEXT | Text work defaults to the CHEAP tier; escalation and grading no longer use premium reasoning models | **P0** | ✅ | 0 | REQ-ESCALATE-2 | 2 pt | **Extreme — 100% of day-one paid spend went to TEXT on claude-sonnet-4-5 ($1.46 writing + $0.16 grading) while $0.00 reached images, video or voice** | Tier param on llm.chat; grader unpinned | 2026-07-24 | v5.10.8. DEC-048 |
| REQ-HEYGEN | Evaluate HeyGen for presenter/avatar video | P3 | 🔵 | 4 | first revenue | 3 pt | Low now — faceless format needs no avatar; API is $1-5/min vs ~$0.02 for stills+motion | New provider integration | — | Researched 2026-07-24: not recommended pre-revenue |
| REQ-UPLOAD-GUARD | CI preflight naming missing files after a partial zip upload | P1 | ✅ | 1 | none | 2 pt | High — a partial upload broke main and reported as 3 unrelated test failures with no mention of a missing file | preflight.py + CI step before pytest | 2026-07-24 | v5.10.9. DEC-049 |
| REQ-REUPLOAD-5107 | Re-upload pipeline/workers/departments/portfolio.py from the v5.10.7 zip | **P0** | 🔴 | — | founder | 1 pt | **CI red on main; backpressure not actually deployed** | none | — | Verified missing at commit 9077207 |
| REQ-NOCACHE-DB | Next.js was caching every Supabase query separately — server reads returned snapshots minutes-to-hours old | **P0** | ✅ | 1 | none | 1 pt | **Extreme — this is the root cause of the "worker is not beating" banner, the frozen Spend card, and every stale dashboard number across the whole session** | global fetch with cache:"no-store" on both clients | 2026-07-24 | v5.11.0. DEC-050 |
| REQ-MCP-URL-TOLERANT | Accept the MCP token as a URL path segment instead of 404ing | P1 | ✅ | 1 | REQ-MCP-OAUTH | 1 pt | High — three connection attempts failed on a formatting typo that looked like an auth error | Catch-all route promotes a path token to a Bearer header | 2026-07-24 | v5.11.1. DEC-051 |
| REQ-BUDGET-3 | Raise proving budgets (daily $5.00, per-account $1.50) | P1 | ✅ | 1 | REQ-LEDGER-DEDUPE | 0 pt | Medium — gives escalation room now that real spend is ~$0.34/day | data only, applied via SQL | 2026-07-24 | Founder-approved; applied directly |
| REQ-MCP-TOOLNAMES | MCP tool names must match ^[a-zA-Z0-9_-]{1,64}$ — dots are rejected | **P0** | ✅ | 1 | REQ-MCP-OAUTH | 1 pt | **Extreme — the connector authorised, then every message in the conversation was blocked by tool-list validation** | 16 tools renamed agentx.x -> agentx_x, dotted form still accepted | 2026-07-24 | v5.11.2. DEC-052 |
| REQ-BUILD-FIX | Remove `export { handle }` from route.ts — it broke every Vercel build | **P0** | ✅ | 1 | REQ-MCP-TOOLNAMES | 1 pt | **Extreme — 4 consecutive deploys errored, so the tool-name fix never reached production and the founder stayed locked out of chat** | Next.js route files allow only GET/POST/… + config exports | 2026-07-24 | v5.11.3. DEC-053 |
| REQ-MCP-DOCS-SYNC | Settings panel + route header still documented dotted tool names | P2 | ✅ | 1 | REQ-MCP-TOOLNAMES | 1 pt | Low direct, high confusion — the founder read the UI list and reasonably concluded the fix had not shipped | Cosmetic text only; protocol was already correct | 2026-07-24 | v5.11.4 |
| REQ-TOPIC-DEDUPE | Never re-pick a topic already on the board (14-day window, emoji/case-insensitive) | P1 | ✅ | 1 | none | 2 pt | High — 5 drafts of one topic and 3 of another, each a separate paid write + grade + approval-queue slot for the SAME idea | Filter before any spend | 2026-07-24 | v5.11.5. DEC-054 |
| REQ-REJECT-REASONS | Add "duplicate topic", "not my brand", "other" to the reject options | P2 | ✅ | 1 | none | 1 pt | Medium — no honest way to reject 7 repeats; a wrong reason actively mistrains the writer | UI list | 2026-07-24 | v5.11.5 |
| REQ-DRAFT-COPY | "No browser preview (worker had no Supabase storage)" implied a failure that was not happening | P2 | ✅ | 1 | none | 1 pt | Medium — a healthy pipeline looked broken; the founder asked "where are the reels" | Copy fix | 2026-07-24 | v5.11.5 |
| REQ-SCOUT-TITLES | Scout uses competitor captions verbatim as topics ("Can Salish do her skincare in 1 minute?") | P1 | 🟡 | 1 | none | 3 pt | High — topics are other creators' post titles, not original angles | Topic picker rewrite | — | Discovered 2026-07-24 via MCP |
| REQ-NOTOPIC-2 | 8 residual `write_script: no topic` fatals — the v5.9.6 guard misses a path | P2 | 🟡 | 1 | none | 2 pt | Medium — each wastes a claim | Trace remaining spawn path | — | Discovered via agentx_failures |
| REQ-ART-BYPASS | Approve path spawned creative.render directly, skipping art.direct entirely | **P0** | ✅ | 1 | REQ-ARTDIRECTOR-1 | 1 pt | **Extreme — the Art Director shipped in v5.10.0 and has NEVER run on a single item** | Approve now routes art.direct -> render | 2026-07-24 | v5.11.6. DEC-055 |
| REQ-CRASHLOOP-1 | Quarantine a render that kills the worker instead of retrying it forever | **P0** | ✅ | 1 | none | 2 pt | **Extreme — worker OOM'd mid-render, restarted, re-claimed the same job, died again, re-billing ElevenLabs each pass** | Counter on the board item, written before heavy work | 2026-07-24 | v5.11.6. DEC-056 |
| REQ-VOICE-CACHE | Reuse existing narration audio on retry instead of re-paying | P1 | ✅ | 1 | REQ-CRASHLOOP-1 | 1 pt | High — each crash-retry re-billed ElevenLabs for identical audio | Disk check before TTS | 2026-07-24 | v5.11.6 |
| REQ-RENDER-MEMORY | Render exceeds the 512MB container — reduce peak memory or move to its own service | **P0** | 🟡 | 2 | none | 5 pt | **Extreme — this is what is actually preventing a published post** | Stream frames, lower res, or split the heavy lane out | — | Root cause of the crash loop |
| REQ-IDEOGRAM | Implement the ideogram endpoint — key and catalog entry existed, handler did not | P1 | ✅ | 3 | none | 2 pt | High — best model for TEXT IN IMAGE; carousels and thumbnails need legible copy | _call_t2i handler + text_heavy tier | 2026-07-24 | v5.11.7. Founder had already set the key |
| REQ-FEED-DETAIL | Wall-clock time + step/job/item on every feed row | P2 | ✅ | 1 | none | 1 pt | Medium — "3m ago" cannot be correlated with a Railway log or a Supabase row | Columns already existed in agent_events; UI never showed them | 2026-07-24 | v5.11.7 |
| REQ-GRADER-THRESHOLD | Grader rejecting every script at 5.7-6.2 against an 8.0 floor | P1 | 🟡 | 1 | first published post | 2 pt | High — nothing can pass, so nothing renders unless the founder approves manually | Review MIN_GRADE vs cheap-tier output quality | — | Observed in Chat with agents |
| REQ-GRADER-THRESHOLD | Writer prompt now carries the grader's rubric and anchors | P1 | ✅ | 1 | none | 2 pt | High — 3 consecutive scripts scored 6.0/5.7/6.0 vs an 8.0 floor; nothing could pass unaided | Raised the writer, did NOT lower the bar | 2026-07-24 | v5.11.8. DEC-057 |
| REQ-SCOUT-TITLES | Trend titles rewritten into original angles instead of used verbatim | P1 | ✅ | 1 | none | 3 pt | High — board carried other creators' captions naming other creators | free_chat rewrite, deterministic fallback | 2026-07-24 | v5.11.8. DEC-058 |
| REQ-VISUALS-REAL | Every frame fell back to a procedural gradient — no image was ever generated | **P0** | ✅ | 1 | none | 3 pt | **Extreme — all 3 published reels are coloured slides; this is the founder's blocker to approving content** | Free keyless rung before gradient; gradient now loud | 2026-07-24 | v5.11.9. DEC-059 |
| REQ-ART-SUBJECT | Art Director must name a photographable scene, never narration or an abstraction | P1 | ✅ | 1 | REQ-ARTDIRECTOR-1 | 2 pt | High — the fallback used beat narration as the image prompt | Per-niche subject banks | 2026-07-24 | v5.11.9 |
| REQ-REWRITE-NOT-REJECT | Grader should request an edit, not discard, when a script is close | P1 | 🟡 | 1 | REQ-GRADER-THRESHOLD | 2 pt | High — founder: "do not reject just edit" | Rewrite loop exists; raise reject bar | — | Founder-requested |
| REQ-CONTENT-MIX | Per account per day: 1 reel + 1 short + 1 carousel (5+ images) + 5 stories | P1 | 🟡 | 1 | REQ-VISUALS-REAL | 5 pt | High — 3 reels for one account, 0 for the other, 0 carousels, 0 stories | Format planner in portfolio.tick | — | Founder-specified |
| REQ-DUP-HOOK | Reel repeats the hook line at intro and close | P2 | 🟡 | 1 | none | 1 pt | Medium — sounds unintentional | Narration assembly | — | Founder-reported |
| REQ-PUBLISH-HONESTY | publish_* steps run as dry-run yet items are marked published | P1 | 🟡 | 1 | REQ-PUB-TOKENS | 1 pt | High — the board claims published when nothing posted | Status must reflect reality | — | Confirmed in run_ledger |
| REQ-HEALTH-3 | Heartbeat written from the claim loop, not only a job or a daemon thread | **P0** | ✅ | 1 | REQ-HEALTH-1/2 | 2 pt | **Extreme — a working worker showed a 29-minute-dead banner; heartbeat_pulse was NULL** | Structural liveness signal | 2026-07-24 | v5.11.10. DEC-060 |
| REQ-RENDER-MEMORY | Cut the zoompan working surface and cap ffmpeg threads | **P0** | ✅ | 2 | none | 3 pt | **Extreme — 1296x2304 zoompan buffers exceeded the 512MB container** | 1188x2112 @ 24fps, -threads 1 | 2026-07-24 | v5.11.10 |
| REQ-RAILWAY-REDEPLOY | Railway running v5.11.6 while the repo is at v5.11.9 | **P0** | 🔴 | — | founder | 0 pt | **Extreme — every pipeline fix since v5.11.6 is undeployed** | none (dashboard action) | — | Founder must click Redeploy |
| REQ-CONTENT-MIX | Daily format mix per account: 1 reel + 1 carousel + 5 stories | P1 | ✅ | 1 | none | 5 pt | High — 3 reels for one account, 0 for the other, 0 carousels, 0 stories | New story format (light lane, free-only), mix planner in tick | 2026-07-24 | v5.11.11. DEC-061 |
| REQ-STUDIO-CONSOLE | In-site manual creation console (templates, tools, chat, connectors) | P2 | 🔵 | 3 | REQ-CONTENT-MIX | 13 pt | Medium — a manual mode competes with tools the founder already pays for; autonomy is the moat | Large new UI surface | — | Founder-requested; scoped, awaiting go-ahead |
| REQ-SKILL-PROMPTS | Adapt public marketing/content skill repos into our prompt files | P2 | 🟡 | 3 | none | 3 pt | Medium — hooks and humanisation are our weakest quality axes | Prompt-only, no runtime change | — | From founder's 20-repo list |
| REQ-HUMAN-AXIS | 7th grading dimension: does this sound like a person or a model | P1 | ✅ | 1 | none | 2 pt | High — six axes could all pass while the script still read as AI-written | New axis + enforced in the min-dim guard | 2026-07-24 | v5.11.12. DEC-062 |
| REQ-SKILL-TRUNCATION | Skill files were truncated from the start at 3500 chars, silently | P1 | ✅ | 1 | none | 1 pt | High — appending to an over-cap skill did nothing and said nothing, breaking the advertised "drop a SKILL.md in" workflow | Cap raised to 6000, truncation logged + health() | 2026-07-24 | v5.11.12 |
| REQ-SKILL-PROMPTS | Hook craft, beat structure and humanisation added to writer + grader skills | P2 | ✅ | 3 | REQ-SKILL-TRUNCATION | 3 pt | High — hook was the most-rejected axis | Original guidance; third-party files drop in unchanged | 2026-07-24 | v5.11.12 |
| REQ-SKILL-MULTI | Load EVERY .md in a department folder, not just SKILL.md | P1 | ✅ | 1 | REQ-SKILL-TRUNCATION | 2 pt | High — one skill per department was a hardcoded filename, not a design choice | SKILL.md pinned first, budget shared, cuts named | 2026-07-24 | v5.11.13. DEC-063 |
| REQ-AGENT-SCORECARD | Measure whether agents are actually improving, per window | P1 | ✅ | 1 | none | 3 pt | High — "are they getting better" had no answer but opinion | 7 metrics + trend verdict incl. flat/regressing | 2026-07-24 | v5.11.13. DEC-064 |
| REQ-CIRCUIT-ACCT | Per-account circuit breakers | P3 | 🟡 | 5 | REQ-ISOLATION-1 | 8 pt | Medium at scale | Lane-scoped breakers | — | WSJF 1.8 |
| REQ-RENDER-REGION | Multi-region render capacity | P3 | 🟡 | 5 | volume | 8 pt | Low now | Infra | — | WSJF 0.9 |
| REQ-ONBOARD-AUTO | Automated brand onboarding | P3 | 🟡 | 5 | Phase 3 | 8 pt | High at 100+ brands | Self-serve flow | — | WSJF 1.9 |
| REQ-MODEL-NICHE | Learned per-niche model selection | P3 | 🟡 | 5 | real output data | 8 pt | Medium | Requires published history | — | WSJF 1.4 |
| REQ-PUB-TOKENS | Publishing OAuth, one platform end-to-end | P1 | 🔴 | 1 | founder OAuth setup | 8 pt | Extreme — approved content still cannot post | Token storage + refresh | — | CoD 31, 2nd highest |
| REQ-SLASTAGE-1 | Back-planned per-stage deadlines stamped on jobs | P1 | 🟡 | 1 | REQ-SLA-TZ ✅ | 3 pt | High — makes fairness the SLA enforcer | Reuses v5.9.5 ordering | — | DEC-031 |
| REQ-RATELIMIT-1 | Per-provider semaphores + token bucket | P1 | 🟡 | 2 | none | 5 pt | High — prevents 429 storms | MANDATORY before concurrency | — | Blocks REQ-PARALLEL-1 |
| REQ-PARALLEL-1 | claim(limit=N) + thread pool (~8x throughput) | P1 | 🟡 | 2 | REQ-RATELIMIT-1 | 5 pt | Extreme at scale — SLA unreachable without it | Thread-safe clients, bus context | — | TD-05 |
| REQ-LANES-1 | Job lanes light/heavy/free-only + per-lane pools | P1 | 🟡 | 2 | REQ-PARALLEL-1 | 3 pt | Medium — OOM protection at 512MB | Lane classification | — | DEC-030 |
| REQ-CHAIN-1 | Stop self-scheduling chain multiplication | P2 | 🟡 | 3 | none | 3 pt | Medium — cadence fixes currently diluted | Single-chain guard | — | TD-03 |
| REQ-OVERHEAD-2 | Cut self-maintenance share of queue (97%) | P2 | 🟡 | 3 | REQ-CHAIN-1 | 3 pt | Medium — reclaims throughput | Ops job consolidation | — | TD-04 |
| REQ-SCALE-WORKERS | 2nd Railway worker replica | P2 | 🟡 | 3 | REQ-CHAIN-1, REQ-HEALTH-1 ✅ | 5 pt | High at scale | Linear cost, linear throughput | — | Do not precede REQ-PARALLEL-1 |
| REQ-PREP-REDESIGN | Paused prep: subordinate, capped, decay-classified, graded | P2 | 🟡 | 3 | free capacity restored | 5 pt | High — 103 idle accounts compound inventory | Prep governor + classification | — | DEC-032, TD-11 |
| REQ-PREP-PROMOTE | Resume bridge: prep → live funnel | P2 | 🟡 | 3 | REQ-PREP-REDESIGN | 3 pt | Medium — instant production on unpause | Promotion + staleness refresh | — | — |
| REQ-LEARN-1 | Replace 45-min heuristic with measured p75 | P2 | 🟡 | 3 | real published throughput | 5 pt | Medium — ETA accuracy | Rolling percentile store | — | TD-09, DEC-022 |
| REQ-BUDGET-2 | Per-account monthly cap + spend bar in UI | P2 | 🟡 | 4 | none | 3 pt | Medium — founder visibility | Web layer only | — | Cap already enforced since v5.9.4 |
| REQ-SLA-UI | SLA chips + prep filter in Studio | P2 | 🟡 | 4 | REQ-SLA-TZ ✅ | 3 pt | Medium | Web layer only | — | Reads settings.sla_status |
| REQ-PAYOUT-1 | Stripe Connect payout automation (50% affiliate) | P2 | 🟡 | 4 | first revenue | 5 pt | Medium — currently manual | Connect onboarding + webhooks | — | — |
| REQ-DEADCODE-1 | Remove legacy pipeline/agent/, refresh roster docs | P3 | 🟡 | 4 | REQ-PARALLEL-1 | 5 pt | Low direct | High clarity — removes audit confusion | — | TD-01, TD-02 |
| REQ-ISOLATION-1 | Per-account queues + leases | P2 | 🟡 | 5 | ~50 active accounts | 13 pt | High at 500 brands | Schema change, high risk | — | DEC-033, TD-10 |
| REQ-TRADEMARK | Resolve Agent-X vs agentx.so | P1 | 🔵 | — | founder decision | 8 pt | High — blocks marketing spend | none | — | Cheap now, expensive later |
| REQ-MCP-CLAIMS | Scope MCP claims honestly pre-marketing | P2 | 🔵 | 4 | founder review | 3 pt | Medium — honesty policy | none | — | — |
| G-1 | Request tracker missing 5 mandated fields | P2 | ✅ | — | none | 1 pt | Low | Compliance with program spec | 2026-07-24 | This table |
| G-2 | Decision log missing 2 mandated fields | P2 | ✅ | — | none | 1 pt | Low | Compliance | 2026-07-24 | See §2B |
| G-3 | Action plan: BI/EI/Complexity/ROI only on CRITICAL tier | P2 | 🟡 | — | none | 2 pt | Low | Compliance | — | Batch 2 doc pass |
| G-4 | 4 Phase-5 items named but not WSJF-scored | P3 | 🟡 | — | none | 1 pt | Low | Compliance | — | Batch 2 doc pass |
| G-5 | Dashboards graded from repo files, not the deployed site; banner/sidebar requests unverified | P2 | 🟡 | 1 | web access | 2 pt | Medium — two founder requests unconfirmed | Verification only | — | Fold into Batch 2 |
| G-6 | Ambiguity: whether the roadmap authorized immediate implementation | P3 | ✅ | — | founder ruling | 0 pt | Low | Process clarity | 2026-07-24 | Resolved: founder approved execution |

## 2B · DECISION LOG — FULL SCHEMA (closes gap G-2)
Mandated fields: ID · Date · Decision · Reasoning · Alternatives · Expected benefits · Risks · Related items.
(§2 retained verbatim below as history.)

| ID | Date | Decision | Reasoning | Alternatives considered | Expected benefits | Risks | Related |
|---|---|---|---|---|---|---|---|
| DEC-055 | 2026-07-24 | Founder approval routes through art.direct, which then hands off to creative.render | human_desk._approved_sweep spawned creative.render directly. The Art Director built in v5.10.0 therefore never executed on a single item — every frame was rendered from whatever the writer left in beat.visual_prompt, i.e. narration text, which is precisely the "asking an image model to draw a sentence" problem it was built to solve. Two batches of work sat dead behind one missing hop | Call art direction inside render (rejected — couples two departments); leave it to the grading path only (rejected — founder-approved items are the ONLY ones that render) | Every rendered frame now gets a real shot brief | Adds one hop before render → art.direct is fail-open and always spawns the render | REQ-ART-BYPASS, REQ-ARTDIRECTOR-1 |
| DEC-057 | 2026-07-24 | The writer prompt carries the grader's full rubric and score anchors; MIN_GRADE stays at 8.0 | Live evidence: scripts graded 6.0, 5.7, 6.0 and were all rejected with specific, correct feedback. The writer was being judged against a standard it had never been shown. Lowering the floor would ship weaker content and hide the problem | Lower MIN_GRADE to 6.5 (rejected — the grader's criticism was accurate; the scripts genuinely were generic); raise the model tier (rejected — a rubric-shaped prompt is free, a bigger model is not) | Scripts are written to the standard they are measured by; rewrites and their cost should fall | Prompt grows ~1.5KB → negligible against an 1800-token completion | REQ-GRADER-THRESHOLD, REQ-CHEAP-TEXT |
| DEC-063 | 2026-07-24 | A department loads every .md in its folder; SKILL.md is pinned first and the budget is shared with named cuts | The loader hardcoded `<dept>/SKILL.md`, so a department could only ever have one skill — the writer needs hook craft, beat structure, humanisation and niche voice, four concerns that cannot be swapped or disabled independently inside one file. Pinning SKILL.md first means adding a file can never push the foundation out of the budget | One giant file (rejected — the current state, and unmaintainable); unlimited budget (rejected — every character is billed on every call) | Skills become modular; third-party files drop in beside ours | Files can still exceed the budget → mitigated: cuts are named in the log and in TRUNCATED | REQ-SKILL-MULTI |
| DEC-064 | 2026-07-24 | Improvement is measured on 7 metrics per window, and the verdict may return "flat" or "regressing" | The founder asked how we can KNOW the agents are improving. These agents do not self-improve — no weights change. What improves is prompts, skills, memory lessons and founder feedback, each measurable. A scorecard that could only report progress would measure nothing, so "flat" and "regressing" are first-class outcomes | Trust the grader's own scores (rejected — the grader is one of the things being evaluated); founder judgement alone (rejected — does not scale and is not a trend) | Claims about agent quality become falsifiable against a baseline | Metrics can be gamed by the agents → mitigated: approval_rate comes from the founder and cannot be influenced by them | REQ-AGENT-SCORECARD |
| DEC-062 | 2026-07-24 | Grading gains a 7th axis, `human`, enforced by the same min-dimension guard as the other six | The grader scored hook, visuals, pacing, audio, caption and cta — all six could pass while the script still read as machine-written, which is the fastest way to lose short-form attention. Nothing measured it. Adding an unenforced axis would have been decoration, so it is included in the MIN_DIM check | Post-writer "humanise" rewrite pass (rejected — a second model call per script, and it treats the symptom); leave it to the writer prompt (rejected — unmeasured guidance is unenforced) | Machine-sounding scripts are rejected with a specific reason instead of shipping | One more axis makes passing harder → intended: the writer prompt now carries the same rubric | REQ-HUMAN-AXIS, REQ-GRADER-THRESHOLD |
| DEC-061 | 2026-07-24 | Each account owes a daily FORMAT MIX (1 reel, 1 carousel, 5 stories), and the cheap formats are spawned first | Ideation only ever spawned the reel path, so @puppy.parent got three reels, @glowup.daily got nothing, and no carousel or story ever existed. Reels are also the only format that touches ffmpeg — the exact path that was OOM-killing the worker — so a reel-only strategy means one crash equals zero output that day. Stories (~$0.003, one image, no ffmpeg) and carousels (~$0.015) keep an account alive between reels | Post more reels (rejected — highest cost, highest crash risk, and an account posting one video a day reads as abandoned); manual scheduling (rejected — does not scale past two accounts) | An account produces 7 pieces/day across 3 formats; a render failure no longer zeroes the day | More items in the approval queue → backpressure already caps it at 5 unapproved | REQ-CONTENT-MIX, REQ-BACKPRESSURE-1, REQ-RENDER-MEMORY |
| DEC-060 | 2026-07-24 | Worker liveness is written from the claim loop itself; the job and the daemon thread become secondary | Liveness had two signals and both failed silently. Evidence at 02:15: the worker wrote a provider report at 02:12 (3 min earlier) while worker_health had been frozen since 01:46 and settings.heartbeat_pulse was NULL — the v5.9.6 pulse thread was not running at all in the deployed build. Four prior fixes (REQ-VERSION-1/2, REQ-HEALTH-1/2) all aimed at this banner and none made liveness structural | Keep only the thread (rejected — demonstrably absent in production); keep only the job (rejected — dies whenever the queue stalls, the original v5.9.5 fault) | If the claim loop runs, the heartbeat is written; if it stops, the worker really has stopped | One more DB write per 15s → trivial, and rate-limited | REQ-HEALTH-3, REQ-HEALTH-1, REQ-HEALTH-2 |
| DEC-059 | 2026-07-24 | A frame may only fall back to a procedural gradient AFTER a free keyless generator has been tried, and the fallback is recorded as a FAILURE with an explanatory detail | The founder watched the first three published reels and asked why the background was "still colour". run_ledger held the answer: aisuite:t2i "all t2i providers failed: fal timed out", a gemini 429, then rich-gradient-v2 sixteen times at $0 with ok=True. Not one image was generated, and nothing anywhere said so — a reel with no imagery looked like a successful render | Retry the paid ladder harder (rejected — fal timeouts and gemini quotas are not fixed by retrying); accept gradients as a style (rejected — the founder cannot approve content that looks like coloured slides) | Reels get real imagery even when every paid provider is down; when they do not, it is visible in settings.visuals_health | Pollinations is slower and lower fidelity → acceptable: a real scene beats a gradient, and paid providers are still preferred first | REQ-VISUALS-REAL, REQ-ART-SUBJECT |
| DEC-058 | 2026-07-24 | Scraped trend titles are rewritten into neutral topics before entering the board | The picker used trend titles VERBATIM, so the board carried "Can Salish do her skincare in 1 minute?" and "Get unready with me in the Bahamas!" — other creators' captions, naming other creators. That violates the founder's own rule: clone the PATTERN, not the post | Ban trend-sourced topics (rejected — trends are the only legal signal available); manual curation only (rejected — does not scale to 100 brands) | Topics become subjects; competitor names never reach a script | A rewrite can drift from the trend → mitigated: clean titles pass through untouched, and the rewriter is fail-open | REQ-SCOUT-TITLES, REQ-TOPIC-DEDUPE |
| DEC-056 | 2026-07-24 | Render attempts are counted on the BOARD ITEM before heavy work begins; 3 starts without completion quarantines the item | A crash-loop is invisible to the job queue: the container dies before the job row can be updated, so attempts stays 0 and the job is re-claimed forever. Observed live — render at 01:01 killed the worker, Railway restarted it at 01:10, the same job re-ran and died again, each pass re-billing ElevenLabs | Rely on queue attempts (rejected — provably blind to this); lower max_attempts (rejected — same blindness) | A poisoned render stops after 3 tries with a readable reason instead of burning money indefinitely | A genuinely transient crash costs the item → acceptable: it is recoverable by resetting the item, and the alternative is an unbounded spend loop | REQ-CRASHLOOP-1, REQ-RENDER-MEMORY |
| DEC-054 | 2026-07-24 | Topics are deduplicated against the board (14 days, normalised for case/punctuation/emoji) BEFORE any write is commissioned | The picker reads scouted trends, which return the same headline repeatedly. The founder's approval queue held 5 drafts titled "Black Spots Gone Instantly!" and 3 of "Can Salish do her skincare in 1 minute?" — each one a separate paid script, a separate grade, and a separate decision for the SAME idea. Backpressure then paused production because the queue was "full" of duplicates | Dedupe at approval time (rejected — the money is already spent by then); exact-string match (rejected — emoji and casing defeat it) | Every draft in the queue is a distinct idea; approval throughput stops being wasted | A genuinely recurring topic is blocked for 14 days → acceptable and env-tunable | REQ-TOPIC-DEDUPE, REQ-BACKPRESSURE-1 |
| DEC-053 | 2026-07-24 | A Next.js route.ts exports ONLY HTTP methods and config keys; shared handlers live in web/lib/, never re-exported from a route | v5.11.1 added `export { handle }` so the /api/mcp/[...rest] catch-all could import it. Next validates route exports at build time and rejects anything else, so FOUR deployments errored consecutively. The web layer stayed frozen at v5.11.0 while the repo said 5.11.2 — meaning the tool-name fix was correct in git and absent in production, and the founder kept getting the same connector error against code that no longer existed | Keep the export and disable type checking (rejected — hides real errors); duplicate the handler (rejected — two copies drift) | Builds succeed; the catch-all remains possible via web/lib/mcp/handler.ts | The catch-all is dropped for now → acceptable: it only ever tolerated a malformed URL | REQ-BUILD-FIX, REQ-MCP-URL-TOLERANT |
| DEC-052 | 2026-07-24 | MCP tool names use underscores (agentx_diagnostics); the dotted form is normalised server-side for older clients | Claude validates every remote tool name against ^[a-zA-Z0-9_-]{1,64}$. All 16 tools shipped as "agentx.foo", so the connector completed OAuth, loaded the tool list, and then REJECTED THE ENTIRE CONVERSATION with tools.305.FrontendRemoteMcpToolDefinition.name. The founder could not send messages at all — the most severe possible failure mode from a naming convention | Namespace with a prefix object (not supported by MCP); drop the agentx_ prefix (rejected — collides with other connectors' tool names in a shared list) | The connector is usable; existing Desktop/Cursor configs keep working | A client could still send the dotted name → handled by the normaliser | REQ-MCP-TOOLNAMES, REQ-MCP-DIAG, REQ-MCP-ACTIONS |
| DEC-051 | 2026-07-24 | /api/mcp/<token> is accepted: a trailing path segment beginning axmcp_ or axat_ is promoted to an Authorization header | The connector was registered three times with the token appended to the URL. OAuth completed correctly each time (a client row was written, the account authorised) and Claude still reported "no MCP server was found at the provided URL" — a formatting typo presenting as an auth failure, which is the hardest kind of error to act on | Only document the correct URL (rejected — already done three times); reject with a clearer message (better, but still fails a plainly-intended request) | A malformed-but-unambiguous URL now works; the canonical /api/mcp stays what the UI shows | A token in a URL can leak via logs/referrers → accepted: these are user-scoped, revocable tokens and the alternative was a hard failure | REQ-MCP-URL-TOLERANT, REQ-MCP-OAUTH |
| DEC-050 | 2026-07-24 | Both Supabase clients are constructed with a custom fetch that forces cache:"no-store" | Live /api/version proved it: within ONE response the top-level worker block reported host 24678e1868d7 / v5.8.8 / age 35143s while diag.workers reported host 7eb13f652775 / v5.10.5 / age 3214s, and the real row was host 8a74bd06c517 / v5.10.9 / age 1s. supabase-js calls global fetch(); Next caches per unique request, so each distinct QUERY froze at a different moment. `dynamic = "force-dynamic"` governs the route, not the fetches inside it — which is why three previous fixes did not stop the false "worker is not beating" banner | Add ?t=Date.now() cache-busting per query (rejected — every future query must remember); revalidate tags (rejected — this data must never be cached at all) | Every dashboard number becomes live; a whole class of phantom bugs disappears | Slightly more DB reads → negligible, and correctness outranks it | REQ-NOCACHE-DB, REQ-BANNER-DIAG, REQ-SPEND-DISPLAY, REQ-HEALTH-1 |
| DEC-049 | 2026-07-24 | CI runs a preflight that asserts every shipped release symbol exists, and on failure names the exact file to re-upload | Releases land as web-UI zip uploads, file by file, so a partial upload is a routine failure mode. When v5.10.7's portfolio.py did not land, CI correctly went red — but reported three unrelated test-name failures with no hint that a FILE was missing. The founder cannot act on that output | Checksums per release (rejected — any later edit invalidates them); require git CLI (rejected — the founder deploys via web UI by design); rely on tests alone (rejected — that is the status quo that produced an unreadable failure) | A partial upload now fails in <1s with "RE-UPLOAD THESE FILES: …" | The symbol list must be maintained per release → mitigated: adding entries is part of shipping, and a stale list only under-reports, never blocks a correct tree | REQ-UPLOAD-GUARD |
| DEC-048 | 2026-07-24 | All TEXT generation defaults to the cheap paid tier; premium models must be requested by name. Money follows the CONTENT (images/video/voice), not the chat | Day-one ledger: aisuite.text claude-sonnet-4-5 50 calls $1.4625 (script writing) + grader 12 calls $0.1604 + strategy.audit $0.008 = 100% of paid spend on TEXT, $0.00 on images/video/voice. A 7-beat reel script is a commodity task priced at $0.029/call on a reasoning model when a cheap model does it for ~$0.002 | Keep premium for quality (rejected — the grader rejects weak hooks either way, and quality is set by the prompt and the art direction, not by model tier on a 500-token script); disable escalation (rejected — that returns to zero output) | ~10-30x cheaper text; budget freed for the parts the customer actually sees | Cheap models may write weaker scripts → mitigated: the grader already gates at 8.0 and requests rewrites; if grade-fail rates rise, GRADER_TIER/ESCALATION_TIER can be lifted per step | REQ-CHEAP-TEXT, REQ-LEDGER-DEDUPE |
| DEC-046 | 2026-07-24 | A paid model call is attributed to the ledger EXACTLY once, by the layer that knows the true cost (aisuite); higher layers record the step with cost 0 | run_ledger showed aisuite.text 25 calls $0.8866 and brain 25 calls $0.8866 — the same 25 calls. Every paid write was double-counted, so today read $2.97 when ~$1.50 was spent. Inflated spend trips the daily guard early, hides real headroom, and doubles apparent cost-per-post — the exact metric pricing depends on | Sum distinct steps at read time (rejected — every consumer would need the same de-dup logic); stop recording the step (rejected — loses traceability) | Budget guards act on real numbers; cost-per-post becomes trustworthy | A future caller could re-introduce a double count → mitigated by a test asserting the dedupe branch | REQ-LEDGER-DEDUPE, REQ-COSTPOST-1 |
| DEC-047 | 2026-07-24 | Production pauses per account once MAX_AWAITING_APPROVAL (5) drafts sit unapproved | An unapproved draft has produced zero business value and already cost money. With 11 drafts queued, producing a 12th is spend with no possible return until a human acts. Backpressure is the correct response to a full downstream buffer | Keep producing and let the founder catch up (rejected — that is how 7,412 items were cleared); auto-approve everything (rejected — DEC-038 safety stance) | Spend tracks approval throughput; the queue stays reviewable | A distracted founder stalls production → acceptable and visible: the event names the cause and the cap is env-tunable | REQ-BACKPRESSURE-1, REQ-AUTOAPPROVE-1 |
| DEC-045 | 2026-07-24 | The MCP server becomes a real OAuth 2.1 protected resource (DCR + PKCE, public clients, no client secret) rather than relying on static bearer tokens | Claude.ai's connector dialog has no header field: it performs Dynamic Client Registration against /.well-known/oauth-authorization-server, which did not exist — hence "Couldn't register with AgentX Site's sign-in service". Legacy axmcp_ tokens still work for Cursor/scripts | Ask users to paste a header (impossible — no such field); issue client secrets (rejected — a secret in a desktop client is not a secret, and OAuth 2.1 prefers PKCE); proxy through an existing IdP (rejected — extra dependency for one connector) | The founder can connect Claude directly to Agent-X with a normal sign-in; tokens are per-user, revocable and refreshable | New auth surface = new attack surface -> mitigated: exact redirect_uri matching, mandatory S256 PKCE, single-use 5-minute codes, https-only redirects | REQ-MCP-OAUTH, REQ-MCP-DIAG |
| DEC-044 | 2026-07-24 | Paid escalation is serialised (semaphore, default 1), reserves its estimated cost in the ledger BEFORE calling, re-checks budget inside the lock, and is capped per account per hour | The budget check was correct but RACED. v5.10.2 enabled 4 threads, v5.10.2's backoff release freed 18 parked writer jobs at once, v5.10.3 made escalation actually spend — four threads each read "budget remaining" before any recorded a cost. 36 paid calls, $1.27 in 15 minutes, against a $2.50 daily budget | Optimistic locking on a DB counter (rejected — an extra round-trip per call and still racy across replicas); lower per-account budgets (rejected — treats the symptom); disable concurrency (rejected — throughput is needed) | Spend becomes bounded and predictable under concurrency; worst case per account per hour is ~$0.12 | Serialisation slows escalated writes → acceptable: correctness of spend outranks latency, and free-path writes stay fully parallel | REQ-ESC-THROTTLE, REQ-PARALLEL-1, REQ-ESCALATE-2 |
| DEC-043 | 2026-07-24 | The writer gets an explicit `force_paid` branch calling llm.chat; a missing force_paid is now a LOUD error, never a silent free retry | v5.8.6 made writing free-only (correct then — a paid fallback had burned $0.41 unattended). v5.9.6 added an escalation gate that called write_script(force_paid=True), but brain never accepted the kwarg; the TypeError handler re-ran the SAME free council and logged "paid escalation failed too". settings.escalation_last recorded {"allowed": true} while ZERO paid calls were made | Set ALLOW_PAID_WRITER=1 globally (rejected — removes per-decision governance, exactly what burned $0.41); call the provider SDK directly from creative.py (rejected — duplicates prompt assembly and ledger accounting) | Approved escalations actually produce a script; permission and spend finally agree | A real paid call now happens where none did → bounded by every existing guard, one attempt per job | REQ-ESCALATE-2, REQ-ESCALATE-1 |
| DEC-042 | 2026-07-24 | Free-tier calls are paced by a per-provider token bucket; when a bucket is dry beyond a short cap the ladder SKIPS to the next rung instead of sleeping | 38 x 429 in six hours on a single-threaded worker — nothing paced the free tier. A 5-rung ladder should route around a busy provider, not queue behind it. Also a hard prerequisite: ~8 threads (REQ-PARALLEL-1) against an unpaced tier makes 429s permanent | Global sleep between calls (rejected — punishes healthy providers); provider-side retry only (rejected — that IS the current behaviour); fail closed when dry (rejected — a limiter must never stop production) | Fewer 429s, better free-tier utilisation, concurrency becomes safe to enable | Local pacing can under-use a generous quota → mitigated: env-tunable RPM per provider, adaptive recovery on success | REQ-RATELIMIT-1, REQ-PARALLEL-1 |
| DEC-041 | 2026-07-24 | Art direction is ONE free-council call per reel, structured into shots, and FAIL-OPEN to a deterministic shot list | The renderer reads `beat.visual_prompt`; the writer was putting narration there, so image models were asked to draw a sentence. But this platform already lost its entire output history to a step that preferred waiting over shipping — art direction must never become a second such step | One call per beat (rejected — 6x cost for one creative act); paid model (rejected — quality gain does not justify it before a single post exists); block on failure (rejected — that is the v5.9.5 writer bug repeated) | Frames become shots, style stays consistent across a reel, cost ~$0 | Deterministic fallback is less creative than a model → acceptable: a rendered reel beats a perfect prompt that never renders | REQ-ARTDIRECTOR-1 |
| DEC-039 | 2026-07-24 | ONE version constant, canonical file at `web/version.json`, read by both Python and Next.js | Three independent versions existed (runner.VERSION, hardcoded WEB_VERSION, heartbeat job payload). The banner showed 5.9.4 while 5.9.7 was deployed, making every deploy look failed and deploy verification impossible | Repo-root version.json (rejected — if Vercel's root dir is `web/`, the file is outside the build root and breaks it); env var (rejected — set in two dashboards, drifts again); keep three (rejected — the reported bug) | Bump one file, banner and worker agree; deploys become verifiable | Python must search for the file across layouts → mitigated with a candidate path list + constant fallback | REQ-VERSION-1, TD-15 |
| DEC-040 | 2026-07-24 | Loose documentation lives in `docs/` (active) and `docs/archive/` (historical); nothing but README.md at repo root; no non-page files inside `web/app/` | 32 loose .md/.txt at root plus a stray `.tsx` and `.tsx.txt` INSIDE the Next.js app directory. Docs are inert for the build, but files in `web/app/` are not — a stray `.tsx` there is a real build risk | Delete history outright (rejected — violates the append-only ledger rule); leave as-is (rejected — founder-reported confusion + genuine build risk) | Repo is legible; app dir contains only routable files | Archived paths change → mitigated: nothing imports them | REQ-REPO-HYGIENE, TD-13 |
| DEC-038 | 2026-07-24 | Auto-approve is OFF by default, opt-in per tenant, held to a stricter bar (8.5) than the ship floor (7.0); any error fails CLOSED to the human gate | Removing the founder from the publish path is a safety decision, not a throughput tweak. Silent autonomous publishing under a no-income-claims policy is a brand and platform risk | Auto-approve everything above ship floor (rejected — 7.0 ships mediocre content unreviewed); keep the human gate forever (rejected — it is the Phase-1 ceiling) | Founder lifts the throughput ceiling deliberately, per tenant, when confident | A high grade is not a policy check → mitigated: risk_flags always force human review | REQ-AUTOAPPROVE-1 |
| DEC-034 | 2026-07-24 | Autonomous config MUST merge with a hardcoded floor, never replace; alert on shrink | arena_scout narrowed the free ladder 5→2 with no signal, causing total output failure | Forbid dynamic config (rejected — loses adaptivity); validate schema only (insufficient — a valid 2-rung ladder is still fatal) | Free capacity can never be silently removed; adaptivity retained | Floor may contain stale models → mitigated by `_resolve()` live-model matching | REQ-LADDER-FLOOR, R-1 |
| DEC-028 | 2026-07-24 | Add an SLA-aware paid escalation rung; delay becomes last resort | Router degraded downward with no rung up; $23.85 of $25 cap unused while output was zero | Always-paid (rejected — cost); manual trigger (rejected — needs a human awake) | First published post becomes achievable; cost stays capped | Escalation could multiply spend under concurrency → mitigated by per-thread guards + REQ-DEDUPE-1 | REQ-ESCALATE-1, R-3 |
| DEC-035 | 2026-07-24 | Stalled items stop counting as in-flight after INFLIGHT_MAX_AGE_H (6h) | Governor computed need = quota − produced − inflight; stalled items froze ideation permanently | Clear items aggressively (rejected — destroys recoverable work); disable governor (rejected — churn returns) | Funnel self-recovers when downstream stalls | Age-out could double-produce if the stalled item later completes → bounded by quota re-check | REQ-GOV-2, N-4 |
| DEC-036 | 2026-07-24 | Liveness reported by a daemon thread, not only by a queued job | Single-threaded worker blocked on a slow LLM call also blocked its own heartbeat job → dashboard declared a healthy worker dead | Shorter heartbeat interval (rejected — same blockage); external uptime pinger (rejected — extra service) | Health signal becomes trustworthy immediately | Two sources of truth for liveness (accepted as TD-06); collapses at Phase 2 | REQ-HEALTH-1, N-3, TD-06 |
| DEC-037 | 2026-07-24 | Maintain a separate append-only technical debt register naming the *interest* of each debt | Debt was previously scattered in prose and quietly absorbed into "later" | Track debt as normal roadmap items (rejected — outcompeted by features every time) | Debt stays visible and costed | Register can rot if not updated per release → updated in the same commit as the code | TD-01…TD-12 |

## 2 · DECISION LOG (append-only)

| ID | Date | Decision | Reasoning / Alternatives / Risks |
|---|---|---|---|
| DEC-021 | 2026-07-23 | Ideation is **demand-governed with windowed idempotency** (`ideate:{acct}:{bucket}`, bucket = now // IDEATE_COOLDOWN_S) rather than slowing the tick | Tick also drives sweeps/carousels/CFO checks — slowing it breaks other duties; windowed keys make floods *structurally* impossible independent of cadence. Alternatives: slower tick (rejected), DB unique constraint (needs migration). Risk: window too long delays refill after clears → mitigated: governor re-checks need every tick; the window only caps *job creation*. |
| DEC-022 | 2026-07-23 | SLA classification uses a **simple minutes-per-post heuristic** (45 min default, env-tunable), not learned cycle times | Zero data exists on real cycle times (0 published); heuristic is honest + tunable; learning version deferred until real throughput data accumulates. Alt: percentile ETA from job history (no history yet). Risk: mis-tuned estimate only affects at_risk earliness, never breach correctness. |
| DEC-023 | 2026-07-23 | Fairness implemented as **candidate-page reorder**, CAS untouched | Preserves proven multi-worker safety; O(page) cost; degrades to old order on any error. Alt: SQL-side ordering (supabase-py can't express it); per-account queue tables (Phase 7). Risk: page-local fairness only — acceptable at current volumes; Phase 7 supersedes. |
| DEC-024 | 2026-07-23 | Paused prep = **free_chat-only, hard-guarded, test-enforced** | Founder's $0 mandate for paused accounts; a source-level test asserts no paid path is reachable from `_run_task`. Alt: cheap-tier routing (could leak paid calls). Risk: free providers all down → prep silently skips (correct: $0 > progress). |
| DEC-025 | 2026-07-23 | Prep scripts stored as board `status='prep'`, excluded from quota/in-flight | Keeps prepared inventory visible in Studio without distorting live production math. Alt: separate table (migration + UI work). Risk: Studio needs a 'prep' filter chip (REQ-SLA-UI). |
| DEC-026 | 2026-07-23 | The Master Ledger lives **in the repo** (docs/LEDGER.md), updated in every release zip | The chat-only ledger was lost when the prior session was cut off mid-delivery and the sandbox reset — a repo file survives both. Alt: DB table (harder for founder to read/diff). Risk: none material; requires each zip to include the updated file. |

## 3 · MEASURED BASELINE the v5.9.5 release attacks (7-day window → 2026-07-23)
- 7,404 board items cleared / 4 surviving; 3,622 ideations; 7,248 plan_one; 19 scripts; **0 published**
- $15.17 total LLM spend, ~$12 of it in the strategy/ideation churn loop
- 14,370 human_desk.sync + 4,626 killswitch + 2,271 heartbeat overhead jobs
- 9 × `creative.write_script: no topic` fatals; 1 stuck in_progress job
- 103/105 accounts paused; 2 unpaused at status='ready'

## 4 · RELEASE LOG
- **v5.9.5 (2026-07-23)** — demand-governed ideation; SLA plan/monitor/breach-escalation/self-heal; fair claim ordering; $0 paused-account prep; empty-topic root-cause fix; human_desk 20s→120s. Files: agentcore/jobs.py, workers/runner.py, workers/departments/{portfolio,editorial,human_desk,__init__}.py, +NEW departments/{sla,paused_prep}.py, +tests/test_v595_sla_governor.py, +docs/LEDGER.md. Tests 96/96.
- v5.9.4 — $25/month per-account hard cap in ceo_decide. (Earlier history: see runner.py version comment chain + prior session summaries.)

## 5 · STANDING RULES (founder-mandated engineering program)
1. No Silent Omission — every request ends ✅ / 🟡 / 🔵 / 🔴, tracked by REQ id.
2. Ledger is append-only; removal requires explicit founder approval.
3. On response limits: finish the logical unit, ledger the remainder, wait for "Continue".
4. Every audit/implementation ends with an Instruction Verification Report.
5. **Do not start Batch #2 until the founder explicitly approves after reviewing this package.**
